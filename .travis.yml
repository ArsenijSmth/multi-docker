sudo: required
services:
  - docker

before_install:
  - docker build -t arsenijs/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true arsenijs/react-test npm test

after_success:
  - docker build -t arsenijs/multi-client ./client
  - docker build -t arsenijs/multi-nginx ./nginx
  - docker build -t arsenijs/multi-server ./server
  - docker build -t arsenijs/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push arsenijs/multi-client
  - docker push arsenijs/multi-nginx
  - docker push arsenijs/multi-server
  - docker push arsenijs/multi-worker